import { AppBar, Toolbar, Typography } from "@mui/material";

const Navbar = () => {
  return (
    <AppBar position="static">
      <Toolbar>
        <Typography>Viral Nation</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Navbar;
